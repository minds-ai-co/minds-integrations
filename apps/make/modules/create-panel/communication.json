{
  "url": "/panels",
  "method": "POST",
  "type": "json",
  "body": {
    "name": "{{parameters.name}}",
    "isLinkSharingEnabled": "{{ifempty(parameters.isLinkSharingEnabled, false)}}"
  },
  "response": {
    "output": "{{body.data}}"
  }
}
