{
  "name": "@minds/slack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "npm run build --workspace @minds/mcp-client && tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node test/run.mjs",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@minds/mcp-client": "0.1.0",
    "@slack/bolt": "5.1.0",
    "google-auth-library": "11.0.2",
    "pg": "8.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.15.0"
  }
}
